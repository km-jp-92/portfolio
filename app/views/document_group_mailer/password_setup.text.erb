下のリンクからパスワードを設定してください。
パスワード設定リンクの有効期限は1時間です。また、パスワード設定は一度だけです。
<%= @new_url %>

---

PDFアップロード：
<%= @upload_url %>

PDF閲覧・同期：
<%= @viewer_url %>

グループコード: <%= @group_code %>
グループコードはパスワードを忘れた場合の再発行時に必要です。
