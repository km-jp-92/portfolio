まず、下記リンクから、PDFアップ・同期ページに入るためのパスワードを設定してください（有効期限1時間）
<%= @new_url %>

------------------------------
PDFアップロード・同期リンクの有効期限：<%= @document_group.upload_token_expires_at.strftime("%Y-%m-%d %H:%M") %>
PDFアップロード：
<%= @upload_url %>

PDF閲覧・同期：
<%= @viewer_url %>

------------------------------

パスワード再設定はこちら（グループコードが必要です）：
<%= new_document_group_password_reset_url %>
グループコード: <%= @group_code %>

------------------------------
トップページへ：
<%= root_url %>
