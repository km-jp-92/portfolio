<div class="min-h-screen flex flex-col items-center justify-center bg-gray-50 px-4">

  <% if flash[:alert] %>
    <p class="text-red-600 mb-4"><%= flash[:alert] %></p>
  <% end %>

  <%= form_with url: document_group_password_resets_path, data: { turbo: false }, class: "w-full max-w-md bg-white p-6 rounded shadow" do |f| %>
    <div class="mb-4">
      <%= f.label :email, "メールアドレス", class: "block font-medium text-gray-700 mb-1" %>
      <%= f.email_field :email, class: "mt-1 block w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500" %>
    </div>

    <div class="mb-4">
      <%= f.label :group_code, "グループコード", class: "block font-medium text-gray-700 mb-1" %>
      <%= f.text_field :group_code, class: "mt-1 block w-full border border-gray-300 rounded-md p-2 focus:ring-blue-500 focus:border-blue-500" %>
    </div>

    <div>
      <%= f.submit "パスワード再設定メールを送信", class: "w-full bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-600 transition-colors" %>
    </div>
  <% end %>
</div>
